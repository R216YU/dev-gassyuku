{
  "scripts": {
    "build": "esbuild lambda_function.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "package": "pnpm build && cd dist && tar -acf lambda.zip index.js"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1030.0",
    "@aws-sdk/client-s3": "^3.1030.0"
  }
}
